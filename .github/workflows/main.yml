on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Update repo
        continue-on-error: true
        run: git pull
      - name: Rebuild and restart docker
        run: docker-compose up -d --build
